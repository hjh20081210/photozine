server:
  # Railway 会注入 $PORT 环境变量；本地没设时回退到 8080
  port: ${PORT:8080}

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

upload:
  # 容器/本地上传目录；Railway 可把 Volume 挂到 /app/upload 做持久化
  path: ${UPLOAD_PATH:./upload/}